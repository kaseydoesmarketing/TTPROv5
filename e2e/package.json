{
  "name": "ttprov5-e2e-tests",
  "version": "1.0.0",
  "description": "End-to-end tests for TitleTesterPro v5 authentication flow",
  "main": "index.js",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "test:report": "playwright show-report",
    "install-browsers": "playwright install",
    "test:ci": "playwright test --reporter=github",
    "test:auth-flow": "playwright test auth-flow.spec.ts"
  },
  "keywords": ["e2e", "playwright", "authentication", "firebase"],
  "author": "TitleTesterPro Team",
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "dotenv": "^16.0.0"
  }
}